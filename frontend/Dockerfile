# TODO (Vai - Issue 3): Define la imagen base, copia tu código del dashboard, instala dependencias y expón el puerto de tu UI.
FROM alpine:3.20
WORKDIR /app
CMD ["sh", "-c", "echo Frontend scaffold listo; sleep infinity"]
