# TODO (Alejo - Issue 4): Define imagen Python slim, instala FastAPI/Uvicorn, copia la estructura hexagonal y configura el CMD.
FROM python:3.12-slim
WORKDIR /app
CMD ["python", "-c", "print('API scaffold listo')"]
